{
	"name": "distillogue",
	"description": "A tool for conducting dialogue in the distilled format of your choice; currently in very early development.",
	"author": "Eve Aviv Keinan",
	"license": "MIT",
	"scripts": {
		"installAll": "npm install && cd shared && npm install && cd ../backend && npm install && cd ../frontend && npm install && cd ../testing && npm install",
		"build": "cd backend && npm run build && cd ../frontend && npm run build",
		"buildDev": "cd backend && start npm run buildDev && cd ../frontend && start npm run buildDev",
		"host": "cd backend && start npm run host",
		"hostDev": "cd backend && start npm run hostDev",
		"serve": "npm run build && npm run host",
		"serveDev": "npm run buildDev && npm run hostDev",
		"playwright": "cd testing && npm run playwright",
		"test": "npm run serve && npm run playwright",
		"docker": "docker-compose up",
		"dockerDev": "docker-compose -f compose.yaml -f compose.devOverride.yaml up",
		"dockerPlaywright": "docker-compose -f compose.tests.yaml up",
		"dockerTest": "docker-compose up --detach && docker-compose -f compose.tests.yaml up",
		"prettier": "npx prettier --write ."
	},
	"homepage": "https://github.com/EvAvKein/Distillogue#readme",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/EvAvKein/Distillogue.git"
	},
	"bugs": {
		"url": "https://github.com/EvAvKein/Distillogue/issues"
	},
	"devDependencies": {
		"prettier": "2.8.2"
	}
}
