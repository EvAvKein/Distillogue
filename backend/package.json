{
	"type": "module",
	"scripts": {
		"database": "start mongod",
		"build": "tsc",
		"buildDev": "tsc --watch",
		"server": "node dist/backend/src/server.js",
		"serverDev": "nodemon dist/backend/src/server.js",
		"buildAndServerDev": "npm run buildDev & npm run serverDev",
		"host": "npm run database && npm run server",
		"hostDev": "npm run database && npm run serverDev",
		"serve": "npm run build && npm run host",
		"serveDev": "start npm run buildDev && npm run hostDev"
	},
	"dependencies": {
		"express": "^4.17.2",
		"helmet": "^6.0.0",
		"mongodb": "^5.3.0",
		"uuid": "^9.0.0",
		"zod": "^3.21.4",
		"zod-validation-error": "^1.1.0"
	},
	"devDependencies": {
		"@types/express": "^4.17.13",
		"@types/node": "^18.8.4",
		"@types/uuid": "^8.3.4",
		"nodemon": "^2.0.22",
		"typescript": "^4.4.4"
	}
}
